#pragma once

#include "Tokenizer.h"

/*
enum GrammarNodeType{
};

class GrammarNode;

typedef std::vector< GrammarNode* > GrammarNodes;


class GrammarNode{
public:
    enum Type{
        CONST,
        VALUE,
        SEQUENCE,
        CHOICE
    };

private:
    GrammarNodeType type_;
    GrammarNodeLinks items_;
};



class GrammarBase{

    enum NodeType{
        EMPTY,
        MATCH,
        SEQUENCE,
        CHOICE

    };

    struct NodeBase{

    };

    struct NodeEmpty{
    };

    struct NodeMatch{
        //Action match_;
        //Action action_;

        operator>>(Node& node)
    };


    class NodeSequence{

        NodeRefs items_;
    };

};

class Parser{
};

*/
